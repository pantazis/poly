# Cline Self-Rules

1. **Always read relevant files before executing commands.**
   - Check current docs, configs, and logs first.
   - Never run commands before understanding context.

2. **Always keep documentation updated after changes.**
   - If behavior/process changes, always update `logs/oblm/how_bot_works.md`.
   - Keep updates short, clear, and aligned with real runtime flow.

3. **Before finishing a task, verify consistency.**
   - Confirm rule changes match implementation and docs.
   - Ensure no stale instructions remain.

